<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_assistant_sheet"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="18dp"
    android:paddingBottom="28dp">

    <!-- Top Drag Handle & Status Header -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_alignParentStart="true">

            <!-- Glowing Cyber Arc Indicator -->
            <View
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:background="@drawable/shape_glowing_dot"
                android:layout_marginEnd="8dp" />

            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⚡ FRIDAY ASSISTANT"
                android:textColor="#38bdf8"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.08" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/btnClose"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:tint="#94a3b8"
            android:layout_alignParentEnd="true"
            android:contentDescription="Close" />
    </RelativeLayout>

    <!-- User Speech Transcript -->
    <TextView
        android:id="@+id/tvUserTranscript"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Listening for voice command..."
        android:textColor="#f8fafc"
        android:textSize="17sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="8dp" />

    <!-- Progress Indicator -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:indeterminate="true"
        android:visibility="gone"
        android:layout_marginBottom="8dp" />

    <!-- Assistant Streaming Answer -->
    <TextView
        android:id="@+id/tvAssistantAnswer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="How can I help you, Sir?"
        android:textColor="#cbd5e1"
        android:textSize="14.5sp"
        android:lineSpacingMultiplier="1.25" />

</LinearLayout>
