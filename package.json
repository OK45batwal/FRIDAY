{
  "name": "friday",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/desktop run dev",
    "build": "npm --prefix apps/desktop run build",
    "test": "npm --prefix apps/desktop test",
    "dist:mac": "npm --prefix apps/desktop run dist:mac",
    "dist:win": "npm --prefix apps/desktop run dist:win",
    "android:sync": "npm --prefix apps/desktop run android:sync",
    "backend": "cd services/core && PYTHONPATH=../../ ./venv/bin/uvicorn services.core.app.main:app --host 0.0.0.0 --port 8000 --reload"
  }

}
